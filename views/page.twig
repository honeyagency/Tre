{% extends "base.twig" %}
{% block content %}
	<article class="post-type-{{post.post_type}}" id="post-{{post.ID}}">
		<section class="spacing--gutter spacing--vert-2">
			<h1>{{post.title}}</h1>
			{{post.content}}
		</section>
	</article>
{% endblock %}