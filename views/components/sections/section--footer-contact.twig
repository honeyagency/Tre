{% if options.footer.contact.form != null %}
	<section id="connect" class="section--footer-contact spacing--gutter bg--pink color--black block--angle angle--right before">
		<h3 class="grid--one text--center font--display font--light">{{options.footer.contact.title}}<hr class="color--black"></h3>
		{{ function('do_shortcode', options.footer.contact.form) }}
	</section>
{% endif %}