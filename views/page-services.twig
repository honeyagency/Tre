{% extends "base.twig" %}
{% block content %}
	<article class="post-type-{{post.post_type}} spacing--vert-3" id="post-{{post.ID}}">
		{% for service in services %}
		<section class="section--service flex flex--wrap justify--around spacing--gutter spacing--vert-2 max-wrap margin-center align--items-baseline">
			<div class="grid--one grid--sm-half grid--md-two-thirds column--md-2">
				<h3 class="title--stack color--pink">{{service.title}}</h3>
					{{service.content}}
			</div>
			<div class="grid--one grid--sm-third grid--md-fourth">
				{# {% set project = home.header.featured_project %}
				<a class="block--project bg--black bg-image" href="{{project.link}}" style="background-image:url('{{project.postinfo.grid_image|resize(1304)}}')">
					<div class="project--meta">
						<h2 class="color--white">{{project.title}}<br><span class="font--light">{{project.postinfo.date|date('Y')}}</span></h2>
						<div class="block--categories color--pink font--display text--right">
							<hr class="color--pink">{% for category in project.categories|reverse %}<span class="category">{% if loop.index > 1 %}+ {% endif %}{{category.name}}</span>{% endfor %}
						</div>
					</div>
				</a> #}
				{{service.details}}
			</div>

		</section>

	{% endfor %}
		
	</article>
	{% include 'components/sections/section--footer-contact.twig' %}
{% endblock %}